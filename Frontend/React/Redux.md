## Concept: Single immutable state tree

Basically a signle JavaScript object that contains the state of the app. All mutations to the state are explicit and because of that it is possible to track changes.